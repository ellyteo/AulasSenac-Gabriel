algoritmo "Primeiro Programa"
var
  numero : inteiro

inicio

  escreval("Múltiplos de 3 entre 3 e 30:")
  numero <- 3
  enquanto (numero <= 30) faca
    se (numero mod 3 = 0) entao
      escreval(numero, " ")
    fimse
    numero <- numero + 1
  fimenquanto

fimalgoritmo