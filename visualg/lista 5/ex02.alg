algoritmo "Primeiro Programa"
var
  numero, contador : inteiro

inicio

  escreval("Números pares entre 1 e 20:")
  numero <- 1
  para contador de 1 ate 20 passo 1 faca
    se (numero mod 2 = 0) entao
      escreval(numero, " ")
    fimse
    numero <- numero + 1
  fimpara

fimalgoritmo